# v1.2 轨迹优化修订版

- 逐字审阅提供独立的“全选标点”和“取消标点”，不改变汉字、字母的选中状态。
- 确认保留项后自动进入训练；进度显示在“整体完成度”卡片。可计数的阶段显示已处理/总数；模型训练等暂不可计数的阶段显示动态等待和具体阶段，不编造百分比。完成后同一卡片显示样本覆盖率。
- 像素骨架进行保形简化和局部圆滑。只连接共用端点且方向一致的分支，保留折角、闭环、端点和断开的笔画；这不等同于恢复真实书写时序。
- 旧字样在导出时自动平滑；新增字样在提取时平滑。TTF保留轮廓，写字机使用平滑中心路径。
- 字样裁剪边缘的长直方格线保守清理。靠近真实笔画、倾斜或不完整的格线可能仍需人工取消该候选。旧字样需要重新确认保留项并训练才能重新清理格线。
- 补写建议从内置日常用字池筛选；有偏旁缺口但池中没有合适字时，保留缺口，不用生僻字填满建议。
- 未写字输出自动使用已有训练模型，已学到的逐笔偏旁模型权重提高到90%；缺少模型时仍标记为实验几何估计，不声称已经学会该字。

## 使用

关闭旧后端后运行本目录 `dist/handwriting-studio-portable.exe`，刷新网页。不要只复制exe到别处：启动器需要随包源码和data文件。依赖继续使用项目虚拟环境。

已有字样可以直接重新生成轨迹；带格线的字样请重新导入、审阅并训练。实机走笔效果仍需要用实际写字机验证。

## 验证

新增阶梯锯齿、端点、折角、闭环、断笔、交叉路径、字框清理测试，以及标点选择、训练卡片位置、计数/等待切换和覆盖率回填的前端逻辑检查。旧会话194个字的4407段轨迹做了只读数值对比，点数77279降到31126；累计方向变化14847.2降到3233.3弧度。该指标只衡量路径转向，不能替代字形相似度和机器运动验收。
